import React from 'react';

import FormSubmit from '../Form/FormSubmit';

const Home = () => {
    return (
        <div>
            <FormSubmit></FormSubmit>
        </div>
    );
};

export default Home;